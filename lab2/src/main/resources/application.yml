spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/wood_shop

    username: postgres
    password: anosore

    driver-class-name: org.postgresql.Driver

  jpa:
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect